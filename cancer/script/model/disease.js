/** Simple model of the disease. A disease is the class we are trying to predict*/

function Disease(name, learning_type) {
    this.type       = 'Disease';
    this.name       = name;
    this.learning_type = null;
}